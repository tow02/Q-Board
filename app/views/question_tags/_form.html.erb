<%= form_for(@question_tag) do |f| %>
  <% if @question_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@question_tag.errors.count, "error") %> prohibited this question_tag from being saved:</h2>

      <ul>
      <% @question_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question_id %><br>
    <%= f.text_field :question_id %>
  </div>
  <div class="field">
    <%= f.label :tag_id %><br>
    <%= f.text_field :tag_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
