<%= form_for(@room_subject) do |f| %>
  <% if @room_subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room_subject.errors.count, "error") %> prohibited this room_subject from being saved:</h2>

      <ul>
      <% @room_subject.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
